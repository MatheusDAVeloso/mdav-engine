// Eixo no qual a velocidade será invertida quando a entidade alcançar os limites.
// O valor fica explícito na configuração do jogo e ocupa apenas um byte no storage.
export const REVERSE_VELOCITY_AT_BOUNDS_AXIS = Object.freeze({
  X: 0,
  Y: 1,
});
